import React from "react";
import ReactDOM from "react-dom";
//import parts that you need from components

ReactDOM.render(<p>yay react</p>, document.getElementById("root"));
// change to something like the followning
// ReactDOM.render(<TodoApp />, document.getElementById("root"));
